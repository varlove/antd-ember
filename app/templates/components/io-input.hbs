{{input
	class=inputClass
	disabled=_disabled
	type=type
	value=value
	role="input"
	required=required
	max=max
	min=min
    step=step
	data-error=dataError
	maxlength=maxlength
	minlength=minlength
	pattern=pattern
	placeholder=_placeholder
	focus-in="focusIn"
	focus-out="focusOut"
}}
